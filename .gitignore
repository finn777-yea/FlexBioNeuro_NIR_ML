__pycache__/

*.log

# Serialized models and sklearn artifacts
*.joblib
*.pkl
*.pickle

# nirs4all run outputs (fitted models, arrays, sqlite store)
workspace/
*.sqlite
*.parquet

# The dataset lives only at <repo>/Data. Nested per-stage copies are duplicates:
# resolve inputs through paths.data_path() instead of adding one back.
/*/*/Data/
